'use babel';

import MarkdownMakeAListView from '../lib/markdown-make-a-list-view';

describe('MarkdownMakeAListView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
